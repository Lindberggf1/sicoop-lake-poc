# Contribuindo

Obrigado por considerar contribuir com este projeto.

## Pré-requisitos
- Git
- Docker Desktop + Docker Compose
- WSL2 (Windows)
- (Opcional) Python 3 + venv para rodar testes

## Setup local
```bash
make run
make test
